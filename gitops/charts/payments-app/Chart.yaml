apiVersion: v2
name: payments-app
description: Payments application — vault-consul GitOps deployment
type: application
version: 1.1.0
appVersion: "1.0.0"

dependencies:
  - name: lib
    version: 0.1.0
    repository: file://../_lib
