apiVersion: v2
name: secret-stores
description: ExternalSecrets ClusterSecretStore + per-secret ExternalSecret manifests
type: application
version: 0.1.0
appVersion: "1.0"

dependencies:
  - name: lib
    version: 0.1.0
    repository: file://../_lib
