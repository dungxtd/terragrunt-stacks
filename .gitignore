# Terraform
.terraform/
# Keep .terraform.lock.hcl committed for reproducibility
*.tfstate
*.tfstate.*
*.tfplan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Terragrunt
.terragrunt-cache/
.terragrunt-stack/
terragrunt-debug.tfvars.json

# SOPS / secrets
*.dec
*.decrypted
*.sops.yaml.dec
secrets.yaml
secrets.json

# Local env files
set_env_local.sh
*.local.hcl
.env
.env.*
.kubeconfig-ministack

# macOS
.DS_Store

# Editor
.vscode/
.idea/
*.swp
*.swo

# Helm chart dependencies — KEEP committed for ArgoCD multi-source rendering.
# ArgoCD's repo-server does not run `helm dep update`; the .tgz must be vendored.
